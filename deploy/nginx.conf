# ============================================
# Nginx Configuration for TDX Stock System
# ============================================
# 该配置提供 HTTP 访问和反向代理
# 配置 SSL 后会自动升级为 HTTPS
# ============================================

# HTTP 服务器配置
server {
    listen 80;
    listen [::]:80;

    # 服务器名称 - 部署后请替换为你的域名
    server_name _;  # 接受所有域名，配置域名后修改为: your-domain.com

    # 访问日志
    access_log /var/log/nginx/tdx-stock-access.log;
    error_log /var/log/nginx/tdx-stock-error.log;

    # 客户端最大请求体大小
    client_max_body_size 10M;

    # 根路径配置
    location / {
        # 反向代理到 Docker 容器
        proxy_pass http://127.0.0.1:8080;

        # 代理头设置
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # 缓冲设置
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }

    # API 路由优化
    location /api/ {
        proxy_pass http://127.0.0.1:8080;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # API 不缓存
        proxy_buffering off;

        # 超时设置（API 可能需要更长时间）
        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # 静态文件缓存优化
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://127.0.0.1:8080;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        # 静态文件缓存 7 天
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 健康检查端点
    location /health {
        proxy_pass http://127.0.0.1:8080/api/health;
        access_log off;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# ============================================
# SSL 配置说明
# ============================================
# 运行 setup-ssl.sh 脚本后，Certbot 会自动：
# 1. 在此文件中添加 SSL 证书配置
# 2. 将 HTTP 请求重定向到 HTTPS
# 3. 配置 SSL 安全选项
# ============================================
