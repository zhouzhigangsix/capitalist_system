# 🚀 部署到生产服务器 - 快速指南

## 📋 服务器信息

- **IP地址**: `139.155.158.47`
- **用户名**: `root`
- **密码**: `im6HZcPuFxqFf1`

## ⚡ 三步部署

### 第一步: 部署前检查

```bash
cd /Users/zhouzg/Project/tdx-api
bash deploy/check.sh
```

这会检查:
- ✅ 本地工具 (Git, SSH, Rsync, Curl)
- ✅ 服务器连接
- ✅ 项目文件完整性

### 第二步: 一键部署

```bash
bash deploy/server-deploy.sh
```

脚本会自动:
1. 安装 Docker, Nginx, Certbot
2. 上传项目文件
3. 启动服务
4. 配置反向代理

完成后访问: **http://139.155.158.47**

### 第三步: 配置域名和 SSL

1. **购买并配置域名 DNS:**
   ```
   类型: A记录
   主机记录: @
   记录值: 139.155.158.47
   ```

2. **等待 DNS 生效** (10分钟-24小时)
   ```bash
   dig +short your-domain.com
   # 应返回: 139.155.158.47
   ```

3. **配置 SSL 证书:**
   ```bash
   bash deploy/setup-ssl.sh your-domain.com
   ```

完成后访问: **https://your-domain.com**

---

## 🔄 日常操作

### 更新代码

```bash
bash deploy/quick-deploy.sh
```

### 查看日志

```bash
ssh root@139.155.158.47 'cd /opt/tdx-stock && docker-compose logs -f'
```

### 重启服务

```bash
ssh root@139.155.158.47 'cd /opt/tdx-stock && docker-compose restart'
```

---

## 📚 详细文档

完整部署文档: [docs/服务器部署指南.md](服务器部署指南.md)

---

## 🆘 常见问题

### 1. SSH 连接失败

```bash
# 配置 SSH 密钥
ssh-copy-id root@139.155.158.47

# 或使用密码登录
ssh root@139.155.158.47
# 输入密码: im6HZcPuFxqFf1
```

### 2. 服务无法访问

```bash
# 检查服务状态
ssh root@139.155.158.47 'cd /opt/tdx-stock && docker-compose ps'

# 查看日志
ssh root@139.155.158.47 'cd /opt/tdx-stock && docker-compose logs'
```

### 3. SSL 证书申请失败

确保:
- ✅ 域名已正确解析到服务器 IP
- ✅ 服务器 80 端口可访问
- ✅ Nginx 已正确启动

---

**祝部署顺利！** 🎉
